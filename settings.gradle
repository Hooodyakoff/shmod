pluginManagement {
    repositories {
        maven { url = "https://fabricmc.net" }
        mavenCentral()
    }
}

plugins {
    // Перенесли плагин автоскачивания Java сюда, как того требует компилятор
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'shmod'
